apply plugin: 'java'

version = '1.0'

dependencies {
    compile fileTree(dir: 'vendor/main', include: '*.jar')
}

jar {
    manifest {
        attributes("Premain-Class": "org.inescid.gsd.bdajvm.InstrumentationAgent")
    }
}
